export default {
  // world seed ID
  seed: 0,
  // size of each cube
  cubeSize: 20,
  // chunks count - how many chunks we render on screen
  chunks: 4,
  // size of each chunks
  chunkSize: 26,
};
